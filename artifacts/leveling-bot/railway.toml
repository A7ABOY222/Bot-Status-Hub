[build]
builder = "nixpacks"
buildCommand = "cd ../.. && pnpm install && pnpm --filter @workspace/leveling-bot run build"

[deploy]
startCommand = "pnpm run serve"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
